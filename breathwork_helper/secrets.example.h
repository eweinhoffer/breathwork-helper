#pragma once

// Copy to secrets.h and set your local credentials.
// Do not commit secrets.h when sharing this project.

#define WIFI_STA_SSID "YOUR_WIFI_SSID"
#define WIFI_STA_PASS "YOUR_WIFI_PASSWORD"

// HTTP OTA auth credentials
#define OTA_HTTP_USER "ota"
#define OTA_HTTP_PASSWORD "CHANGE_ME_STRONG_PASSWORD"
